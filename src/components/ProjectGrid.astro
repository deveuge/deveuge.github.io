---
---

<div class="project-grid">
  <slot />
</div>

<style>
  .project-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
    margin: 2rem 0;
  }

  @media (min-width: 1024px) {
    .project-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .project-grid :global([data-project-card].hidden) {
    display: none;
  }
</style>
